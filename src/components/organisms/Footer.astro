---
import { Image } from "astro:assets";
import logoImage from "../../assets/logo.png";

/**
 * Footer - Compact & Persistent Design
 * Uses Environment Variables for professional link management.
 */

// Environment Variables with Fallbacks
const solanaExplorerUrl = import.meta.env.PUBLIC_SOLANA_EXPLORER_URL || "https://explorer.solana.com";
const githubUrl = import.meta.env.PUBLIC_GITHUB_URL || "https://github.com";
const xUrl = import.meta.env.PUBLIC_X_URL || "#";
const discordUrl = import.meta.env.PUBLIC_DISCORD_URL || "#";
const docsPath = import.meta.env.PUBLIC_DOCS_URL || "/docs";
const sysStatus = import.meta.env.PUBLIC_SYS_STATUS || "OPERATIONAL";

// Status Color Logic
const statusColors = {
  OPERATIONAL: "#10B981", // Emerald
  UNDER_DEVELOPMENT: "#fbfbfb", // White
  MAINTENANCE: "#F59E0B", // Amber
  DEGRADED: "#EF4444",    // Red
};

const currentStatusColor = statusColors[sysStatus as keyof typeof statusColors] || "#94A3B8";
---

<footer class="sr-footer-compact">
  <div class="container">
    <div class="footer-content">
      <!-- Brand & Copyright -->
      <a href="/" class="footer-brand">
        <Image src={logoImage} alt="SkillRail Logo" class="footer-logo" width={100} quality="max" />
        <span class="brand-copy">&copy; 2026 SkillRail.</span>
      </a>

      <!-- Navigation -->
      <nav class="footer-nav">
        <ul class="footer-links">
          <li><a href={solanaExplorerUrl} target="_blank" rel="noopener noreferrer" class="f-link">SOLANA_EXPLORER</a></li>
          <li><a href={docsPath} class="f-link">DOCS</a></li>
          <li><a href={githubUrl} target="_blank" rel="noopener noreferrer" class="f-link">GITHUB</a></li>
          <li><a href={xUrl} target="_blank" rel="noopener noreferrer" class="f-link">X</a></li>
          <li><a href={discordUrl} target="_blank" rel="noopener noreferrer" class="f-link">DISCORD</a></li>
        </ul>
      </nav>

      <!-- Status Indicator -->
      <div class="footer-status">
        <span class="status-label">SYS_STATUS:</span>
        <span class="status-value" style={`color: ${currentStatusColor}`}>{sysStatus}</span>
        <div class="status-pulse" style={`background: ${currentStatusColor}; box-shadow: 0 0 8px ${currentStatusColor}`}></div>
      </div>
    </div>
  </div>
</footer>

<style>
  .sr-footer-compact {
    position: sticky;
    bottom: 0;
    background-color: #020617;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
    padding: 0.75rem 0;
    margin-top: -1px;
    z-index: 1000;
    backdrop-filter: blur(10px);
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
  }

  /* ============================================
     BRAND
     ============================================ */

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 1rem;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .footer-brand:hover {
    opacity: 0.8;
  }

  .footer-logo {
    height: 24px;
    width: auto;
    filter: brightness(0.9) drop-shadow(0 2px 4px rgba(0, 0, 0, 0.5));
    transition: all 0.3s ease;
    border-radius: 4px;
  }

  .footer-logo:hover {
    filter: brightness(1.1) drop-shadow(0 0 8px rgba(99, 102, 241, 0.4));
    transform: translateY(-1px);
  }

  .brand-copy {
    font-size: 0.75rem;
    color: #475569;
    font-weight: 500;
  }

  /* ============================================
     NAVIGATION
     ============================================ */

  .footer-links {
    list-style: none;
    display: flex;
    gap: 2rem;
    padding: 0;
    margin: 0;
  }

  .f-link {
    font-family: var(--font-family-mono);
    font-size: 0.7rem;
    font-weight: 600;
    color: #94A3B8;
    text-decoration: none;
    letter-spacing: 0.1em;
    transition: color 0.2s ease;
  }

  .f-link:hover {
    color: #6366F1;
  }

  /* ============================================
     STATUS
     ============================================ */

  .footer-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .status-label {
    font-family: var(--font-family-mono);
    font-size: 0.6rem;
    color: #334155;
    font-weight: 800;
  }

  .status-value {
    font-family: var(--font-family-mono);
    font-size: 0.65rem;
    font-weight: 700;
  }

  .status-pulse {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.5); opacity: 0.5; }
    100% { transform: scale(1); opacity: 1; }
  }

  @media (max-width: 768px) {
    .sr-footer-compact {
      padding: 0.3rem 0;
      height: auto;
    }
    
    .footer-content {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
      gap: 0.2rem;
    }

    .footer-brand {
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }

    .footer-logo {
      height: 20px;
    }

    .brand-copy {
      display: inline-block;
      font-size: 0.75rem;
      color: #94A3B8;
    }

    .footer-nav {
      width: 100%;
      order: 3;
      padding-top: 0.25rem;
      border-top: 1px solid rgba(255, 255, 255, 0.03);
    }

    .footer-links {
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
      gap: 0.2rem 1rem;
    }

    .f-link {
      font-size: 0.7rem;
      letter-spacing: 0;
    }

    .footer-status {
      display: flex;
      align-items: center;
      gap: 0.3rem;
    }

    .status-label {
      display: inline-block;
      font-size: 0.6rem;
    }

    .status-value {
      font-size: 0.65rem;
    }

    .status-pulse {
      width: 5px;
      height: 5px;
    }

    .sr-footer-compact .container {
      padding: 0 1rem;
      max-width: none;
    }
  }
</style>
